export const EXTENSION_NAME = "Virtual Repositories";

export enum GlobalStorageKeysForSync {
    repoGlobalStorage = "virtualRepos",
    sortType = "sortType",
    sortDirection = "sortDirection",
}

export enum StorageKeys {
    myRepos = "myRepos",
    starredRepos = "starredRepos",
}